package com.ai.challenge.alexklars.movies.presentation.navigation

object AppDestinations {
    const val MovieListScreenDestination = "movieListScreen"

    const val MovieDetailsScreen = "movieDetailsScreen/{movieId}"
    const val MovieDetailsScreenDestination = "$MovieDetailsScreen/{movieId}"
}
